@model WebSite.Models.Data.Picks.OptionPickLeg

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>OptionPickLeg</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Name)
            @Html.ValidationMessageFor(model => model.Name)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.EntryPrice)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.EntryPrice)
            @Html.ValidationMessageFor(model => model.EntryPrice)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Quantity)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Quantity)
            @Html.ValidationMessageFor(model => model.Quantity)
        </div>

        <p>@Html.Hidden("optionPickId", (int)ViewBag.OptionPickId)
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to Option Pick", "Edit", "OptionPicks", new { id = ViewBag.OptionPickId })
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
